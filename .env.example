# Database Configuration
# PostgreSQL connection string format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=SCHEMA
DATABASE_URL="postgresql://linkvault_app:your_secure_password@localhost:5432/linkvault?schema=app"

# Shadow database for Prisma migrations (optional, only needed for migrate dev)
# SHADOW_DATABASE_URL="postgresql://linkvault_app:your_secure_password@localhost:5432/linkvault_shadow?schema=app"

# Individual database configuration (used by prisma.config.ts)
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USER=linkvault_app
DATABASE_PASSWORD=your_secure_password
DATABASE_NAME=linkvault
DATABASE_SCHEMA=app
