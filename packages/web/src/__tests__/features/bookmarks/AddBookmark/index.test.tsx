describe("# AddBookmark 테스트", () => {
  test("## 1. 기본 렌더링 테스트", async () => {
    /* arrange */

    /* act */

    /* assert */
    expect(result).toBe(expected);
  });

  describe("## 2. URL 입력 테스트", () => {
    test("### 2-1. 일반 입력 테스트", async () => {
      /* arrange */

      /* act */

      /* assert */
      expect(result).toBe(expected);
    });

    test("### 2-2. Validation 테스트", async () => {
      /* arrange */

      /* act */

      /* assert */
      expect(result).toBe(expected);
    });
  });

  describe("## 3. 제목 입력 테스트", () => {
    test("### 3-1. 일반 입력 테스트", async () => {
      /* arrange */

      /* act */

      /* assert */
      expect(result).toBe(expected);
    });

    test("### 3-2. Validation 테스트", async () => {
      /* arrange */

      /* act */

      /* assert */
      expect(result).toBe(expected);
    });
  });

  describe("## 4. Notes 입력 테스트", () => {
    test("### 4-1. 일반 입력 테스트", async () => {
      /* arrange */

      /* act */

      /* assert */
      expect(result).toBe(expected);
    });

    test("### 4-2. Validation 테스트", async () => {
      /* arrange */

      /* act */

      /* assert */
      expect(result).toBe(expected);
    });
  });

  describe("## 5. 태그 조작 테스트", () => {
    test("### 5-1. 일반 입력 테스트", async () => {
      /* arrange */

      /* act */

      /* assert */
      expect(result).toBe(expected);
    });

    test("### 5-2. Validation 테스트", async () => {
      /* arrange */

      /* act */

      /* assert */
      expect(result).toBe(expected);
    });
  });

  describe("## 6. 폼 Submit 테스트", () => {
    /* tests */
  });
});
